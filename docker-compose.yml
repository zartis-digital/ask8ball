# Generated with vcli (0.20.2.0)
version: '3.7'

services:
  ask8ball:
    build:
      context: .
    image: zartisdigital/ask8ball:latest
    restart: always
    environment:
      - SLACK_TOKEN=
      
